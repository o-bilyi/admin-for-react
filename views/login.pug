include head

.login-form-wrap
    form.login-form(action="/api/login" method="post")
        input.name(type="text" name="name" placeholder="name")
        input.pass(type="number" name="password" placeholder="password")
        button.submit-login-form(type="submit") Login
